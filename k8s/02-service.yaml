apiVersion: v1
kind: Service
metadata:
  name: jmix-k8s
  namespace: kl3init-dev
spec:
  type: ClusterIP
  selector:
    app: jmix-k8s
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: hazelcast
      port: 5701
      targetPort: 5701